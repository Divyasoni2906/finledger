{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd finance-frontend && npm install && npm run build && cd ../finance-backend && npm install"
  },
  "deploy": {
    "startCommand": "cd finance-backend && node src/utils/seed.js && node src/app.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
